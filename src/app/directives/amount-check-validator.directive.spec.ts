import { AmountCheckValidatorDirective } from './amount-check-validator.directive';

describe('AmountCheckValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AmountCheckValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
